export function SiteFooter() {
  return (
    <footer className="border-t border-border">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-4 py-8 sm:flex-row sm:items-center sm:justify-between sm:px-6">
        <p className="font-display text-lg font-semibold">Haven Stays</p>
        <p className="text-xs text-muted-foreground">
          Boutique stays, honestly priced. Per-night rates, totals up front.
        </p>
      </div>
    </footer>
  );
}
